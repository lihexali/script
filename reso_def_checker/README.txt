* Make sure you are install perl eviroment*
How to use
MacOSX:
1. brew install ack
or:
1. cp ack /usr/bin
2. chmod +x /usr/bin/ack

Linux:
1. apt-get install ack-grep
or:
1. cp ack /usr/bin
2. chmod +x /usr/bin/ack

Win32(gitbash):
1. cp ack "C:\Git\bin"
2. run gitbash, execute: chmod +x /bin/ack

